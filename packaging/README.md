# packaging

Distribution packaging for mackey: `cargo-deb` / `cargo-generate-rpm` metadata,
the systemd unit, udev rule, D-Bus policy, and polkit policy.

Populated from M1 onward (see `MILESTONES.md`).
